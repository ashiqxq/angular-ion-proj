import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.udemy.angular.ionic.ashiq.rs',
  appName: 'ionicangularproj',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
